int countDigits(int num) {
    int count=0;
    int numog=num;
    while(num>0){
    int digit=num%10;
    num=num/10;
    if(digit==0)
        continue;
    if(numog%digit==0)
        count++;    
}
return count;
}
