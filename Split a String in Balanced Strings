int balancedStringSplit(char* s) {
    int L=0;
    int R=0;
    int count=0;
    int strings=0;
    for(int i=0;s[i]!='\0';i++){
        if(s[i]=='R')
            count++;
        if(s[i]=='L')
            count--;
        if(!count)
            strings++;
    }
    return strings;
}
